import {environment} from '../../environments/environment';

export const SAVE_STORY_URL: string = environment.BaseApiRoute + "/MemoryPost";
export const Get_STORY_URL: string = environment.BaseApiRoute + "/MemoryPost";
